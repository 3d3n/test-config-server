eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultzone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    
# server:
#   port: 9296

welcome:
  message: config server property located v1

PAYMENT:
  CARD: http://CARD-SERVICE
  BANK: http://BANK-SERVICE
